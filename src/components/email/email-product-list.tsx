import React from 'react'

const EmailProductList = (props: any) => {
          return (
                    <>
                              {props.children}
                    </>
          )
}

export default EmailProductList